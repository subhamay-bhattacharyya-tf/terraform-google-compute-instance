# With N2 Standard Machine Type

Provisions a Compute Instance using the `n2-standard-2` machine type (2 vCPUs,
8 GB RAM). N2 offers better price-performance than N1 for most workloads.

## Usage

```bash
terraform init -backend=false
terraform validate
```
