# With Labels

Provisions a Compute Instance with resource labels for cost allocation and
team ownership tracking (`env`, `team`, `cost-centre`).

## Usage

```bash
terraform init -backend=false
terraform validate
```
