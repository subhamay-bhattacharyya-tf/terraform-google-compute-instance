# Complete

Provisions a fully-configured Compute Instance exercising all available
options: `n2-standard-2` machine type, `pd-ssd` boot disk, public IP, network
tags, labels, startup-script metadata, and `allow_stopping_for_update`.

## Usage

```bash
terraform init -backend=false
terraform validate
```
