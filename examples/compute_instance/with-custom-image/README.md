# With Custom Image (Ubuntu 22.04 LTS)

Provisions a Compute Instance using Ubuntu 22.04 LTS instead of the default
Debian 11 image. Boot disk is sized at 20 GB.

## Usage

```bash
terraform init -backend=false
terraform validate
```
