# With Network Tags

Provisions a Compute Instance with `http-server` and `https-server` network
tags to target existing VPC firewall rules allowing ingress on ports 80 and 443.

## Usage

```bash
terraform init -backend=false
terraform validate
```
