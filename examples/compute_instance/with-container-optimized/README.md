# With Container-Optimized OS

Provisions a Compute Instance using Google's Container-Optimized OS (COS)
image. Suitable for running Docker workloads without manual Docker installation.

## Usage

```bash
terraform init -backend=false
terraform validate
```
