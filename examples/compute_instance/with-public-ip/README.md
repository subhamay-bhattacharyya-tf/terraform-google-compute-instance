# With Public IP

Provisions a Compute Instance with an ephemeral external IP address assigned
via `access_config`. Use this when the instance needs direct internet access.

## Usage

```bash
terraform init -backend=false
terraform validate
```
