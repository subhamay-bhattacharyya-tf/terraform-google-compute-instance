# With SSD Boot Disk

Provisions a Compute Instance with a `pd-ssd` boot disk (50 GB) for
latency-sensitive or IOPS-intensive workloads.

## Usage

```bash
terraform init -backend=false
terraform validate
```
