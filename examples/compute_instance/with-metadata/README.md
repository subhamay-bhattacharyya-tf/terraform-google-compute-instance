# With Metadata (Startup Script)

Provisions a Compute Instance with a `startup-script` metadata entry that
installs and starts nginx on first boot.

## Usage

```bash
terraform init -backend=false
terraform validate
```
